// Add JavaScript functionality if needed
document.addEventListener('DOMContentLoaded', () => {
    // Example: Adding an event listener to the 'Read More' button
    document.querySelector('.our-story button').addEventListener('click', () => {
        alert('Read more about our story.');
    });
});
